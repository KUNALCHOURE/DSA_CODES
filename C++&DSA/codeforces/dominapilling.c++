#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    while (t--)
    {
        int n;
        string s;
        cin>>s;
sort(s.begin(),s.end());
       if(s[0]=='T' && )
        

    }
    
}